
////TEST CASES 

Instruction: Place the code below inbetween the document ready function to run one of the scenarios below.

    /*TEST 1: Moving off the grid to the other side
    var location = [0,9];
    var obstacle = [-1, 1];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['f', 'f'], obstacle, gridSize); */

    /*TEST 2: Running into obstacle
    var location = [0,0];
    var obstacle = [0, 2];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['f', 'f', 'r', 'f'], obstacle, gridSize); */

    /*TEST 3: Basic success path forward    
    var location = [0,0];
    var obstacle = [-9, -9];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['f'], obstacle, gridSize); */

    /*TEST 4: Basic success path forward with turn left then forward   
    var location = [0,0];
    var obstacle = [-9, -9];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['f', 'l', 'f'], obstacle, gridSize); */

    /*TEST 5: Basic success path backward  
    var location = [0,0];
    var obstacle = [-9, -9];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['b'], obstacle, gridSize); */

    /*TEST 5: Basic success path backward with left turn then backward 
    var location = [0,0];
    var obstacle = [-9, -9];
    var gridSize = [10, 10];
    var direction = 'N';
    marsRover(location, direction, ['b', 'l', 'b'], obstacle, gridSize); */
